'use client';import {useState} from 'react';import {products} from '@/data/products';import {ProductCard} from '@/components/product-card';export default function Search(){const[q,setQ]=useState('');const results=products.filter(p=>(p.name+' '+p.category).toLowerCase().includes(q.toLowerCase()));return <main className="max-w-[1200px] mx-auto px-5 py-20"><div className="text-[10px] tracking-[.2em] text-gold">DISCOVER</div><h1 className="font-display text-6xl mt-3">Search</h1><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search jewellery..." className="w-full border-b border-black/30 bg-transparent py-6 text-2xl outline-none mt-8"/><div className="flex gap-3 mt-6 text-[10px] uppercase tracking-widest text-black/45">Trending: Rings · Hoops · Pearls · Gold</div>{q&&<div className="grid grid-cols-2 lg:grid-cols-4 gap-5 mt-12">{results.map(p=><ProductCard p={p} key={p.id}/>)}</div>}{q&&!results.length&&<div className="py-24 text-center text-black/50">No results found. Try another search.</div>}</main>}