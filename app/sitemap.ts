import type {MetadataRoute} from 'next';import {products} from '@/data/products';export default function sitemap():MetadataRoute.Sitemap{const base='https://aurelia-jewellery.vercel.app';const routes=['','shop','about','contact','journal','size-guide','jewellery-care','faq','cart','wishlist','account','search'].map(x=>({url:`${base}/${x}`,lastModified:new Date()}));return routes.concat(products.map(p=>({url:`${base}/products/${p.slug}`,lastModified:new Date()})))}