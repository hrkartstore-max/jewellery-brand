import {products} from '@/data/products';import {ProductCard} from '@/components/product-card';export function generateStaticParams(){return ['rings','earrings','necklaces','bracelets','pendants','chains','jewellery-sets'].map(slug=>({slug}))}export default async function Collection({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const title=slug.replaceAll('-',' ');const items=products.filter(p=>p.category.toLowerCase()===title.replace('jewellery sets','').trim());return <main className="max-w-[1440px] mx-auto px-5 md:px-8 py-14"><div className="text-[10px] tracking-[.2em] text-black/40">HOME / COLLECTIONS / {title.toUpperCase()}</div><h1 className="font-display text-6xl capitalize mt-5">{title}</h1><p className="text-black/50 mt-4 max-w-xl">An edited selection of {title}, designed for modern everyday luxury.</p><div className="grid grid-cols-2 lg:grid-cols-4 gap-5 mt-12">{(items.length?items:products).map(p=><ProductCard p={p} key={p.id}/>)}</div></main>}